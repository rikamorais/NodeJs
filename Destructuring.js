// Destructuring (desestruturação) é um método de importação de campos de objetos.
// Quando um objeto é exportado em um módulo podemos importar apenas alguns atributos
// dele utilizando a notação abaixo:

const {appName, appPort} = require('./app-config');